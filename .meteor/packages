# Meteor packages used by this project, one per line.
# Check this file (and the other files in this directory) into your repository.
#
# 'meteor add' and 'meteor remove' will edit this file for you,
# but you can also edit it by hand.

# default packages

#meteor-base@1.5.2                       # Packages every Meteor app needs to have
# meteor-base imples
meteor@2.1.0
webapp@2.0.5
#es5-shim@4.8.1                          # ECMAScript 5 compatibility for older browsers
# Only enable this during development of the landing page, but during development of docs it will cause an infinite reload loop.
#hot-code-push                           # disabled to prevent infinite reload loop when connecting another client (https://github.com/meteor/meteor/issues/13653)
ddp@1.4.2

mobile-experience@1.1.2                 # Packages for a great mobile UX
mongo@2.1.0                             # The database Meteor supports right now
reactive-var@1.0.13                     # Reactive variable for tracker

# Enable all of these for standard Meteor module bundling, disable them when
# testing native Node.js ESM.
ecmascript@0.16.10                      # Enable ECMAScript2015+ syntax in app code

# We don't have this disabled because right now we're building TS files
# ourselves (see package.json scripts).
#typescript@5.6.3                        # Enable TypeScript syntax in .ts and .tsx modules

# Enable production optimization when needed (less is more to start with)
#standard-minifier-css@1.9.3             # CSS minifier run for production mode
#standard-minifier-js@3.0.0              # JS minifier run for production mode

shell-server@0.6.1                      # Server-side component of the `meteor shell` command
#hot-module-replacement@0.5.4            # Update client in development without reloading the page

static-html@1.4.0                       # Define static page content in .html files

# added packages

accounts-password@3.0.3
accounts-ui@1.4.3
# manually add jquery because accounts-ui needs blaze which needs jquery (bug: https://github.com/meteor/meteor/issues/13658)
jquery@3.0.2

session@1.2.2
ferjep:persistent-session@0.4.13        # Enables Session to persist across page reloads using localStorage

zodern:types@1.0.13                     # types for Meteor packages
