{
	"name": "website",
	"private": true,
	"// we want to use type:module, but Meteor code is still CommonJS. So we set type:commonjs, then import our es modules from server/entry.js on the server": "",
	"type": "commonjs",
	"scripts": {
		"start": "npm run _meteor -- -p 8765",
		"build": "tsc",
		"dev": "tsc --watch",
		"_meteor": "meteor --exclude-archs web.browser.legacy",
		"clean": "npm run clean:unix || npm run --script-shell=pwsh clean:windows",
		"clean:unix": "rm -rf `find client -name '*.js*'` && rm -rf `find public -name '*.js*'` && rm -rf `find server -name '*.js*'`",
		"clean:windows": "Get-ChildItem -r -i client | Where{$_.Name -Match '*.\\.js*.'} | rm -r -fo && Get-ChildItem -r -i public | Where{$_.Name -Match '*.\\.js*.'} | rm -r -fo && Get-ChildItem -r -i server | Where{$_.Name -Match '*.\\.js*.'} | rm -r -fo",
		"postinstall": "echo POSTINSTALL && echo && cd public/ && npx npm install",
		"// LUME Scripts below ///////////////////////////////////////": "",
		"typecheck": "lume typecheck",
		"typecheck:watch": "lume typecheckWatch"
	},
	"dependencies": {
		"@babel/runtime": "^7.15.4",
		"@lume/element": "^0.11.0",
		"@tweenjs/tween.js": "^18.6.4",
		"@types/meteor": "^2.6.2",
		"lume": "^0.3.0-alpha.37",
		"meteor-node-stubs": "^1.0.0",
		"solid-js": "^1.4.0",
		"typescript": "^5.0.0"
	},
	"devDependencies": {
		"@lume/cli": "^0.11.0",
		"prettier": "3.0.3"
	},
	"meteor": {
		"mainModule": {
			"server": "server/entry.js"
		}
	}
}
